name: GitHub Actions Demo - main
on:
  push:
    branches: [ main ]
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - uses: sofusalbertsen/custom-action@main
        with:
          myInput: sofus
